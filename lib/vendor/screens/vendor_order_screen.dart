import 'package:flutter/material.dart';

class VenderOrderScreen extends StatelessWidget {
  const VenderOrderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Vender order Screen'),
    );
  }
}
